import React from "react";


const GalleryView = (props) => {
console.log('hello');
    // console.log(props);
    // const gallery = props.galleries.map(gal => {gal.name})
    // console.log(gallery);

    return (
        <div>
            {/* <h1>{gallery}</h1> */}
            <h1>Hello from GalleryView</h1>
        </div>
    )
}

export default GalleryView;